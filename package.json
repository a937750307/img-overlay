{
  "name": "batch-overlay-tool",
  "version": "1.0.0",
  "description": "批量模板覆盖工具 - 纯前端图片批量合成",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.1",
    "vite": "^6.0.0",
    "wrangler": "^4.103.0"
  }
}
